package dao;

import dao.entities.EtatDesLieux;

/**
 * Interface spécifique pour les opérations liées à l'entité {@link EtatDesLieux}.
 * Hérite des méthodes génériques définies dans l'interface {@link DAO}.
 */
public interface EtatDesLieuxDAO extends DAO<EtatDesLieux> {
    // Les méthodes spécifiques à l'entité Etat_des_lieux peuvent être ajoutées ici si nécessaire
}