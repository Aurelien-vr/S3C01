package dao;

import dao.entities.ContratColocation;

/**
 * Interface spécifique pour les opérations liées à l'entité {@link ContratColocation}.
 * Hérite des méthodes génériques définies dans l'interface {@link DAO}.
 */
public interface ContratColocationDAO extends DAO<ContratColocation> {
    // Les méthodes spécifiques à l'entité Contrat_colocation peuvent être ajoutées ici si nécessaire
}


