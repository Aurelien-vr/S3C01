package dao;

import dao.entities.Enumerer;

/**
 * Interface spécifique pour les opérations liées à l'entité {@link Enumerer}.
 * Hérite des méthodes génériques définies dans l'interface {@link DAO}.
 */
public interface EnumererDAO extends DAO<Enumerer> {
    // Les méthodes spécifiques à l'entité Enumerer peuvent être ajoutées ici si nécessaire
}
