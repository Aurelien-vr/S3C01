package dao;

import dao.entities.SoldeDeToutCompte;

/**
 * Interface spécifique pour les opérations liées à l'entité {@link SoldeDeToutCompte}.
 * Hérite des méthodes génériques définies dans l'interface {@link DAO}.
 */
public interface SoldeDeToutCompteDAO extends DAO<SoldeDeToutCompte> {
    // Les méthodes spécifiques à l'entité Solde_de_tout_compte peuvent être ajoutées ici si nécessaire
}
